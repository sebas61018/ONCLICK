<section id="sobre-mi" class="relative py-16 md:py-24 px-6 text-white overflow-hidden">
  <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16 items-center">

    <!-- Imagen responsiva -->
    <div class="flex justify-center md:justify-start">
      <img
        src="/assets/logoHome/logoHome.png"
        alt="Logo ONCLICK"
        class="w-40 h-40 sm:w-52 sm:h-52 md:w-[300px] md:h-[300px] rounded-tl-[100px] rounded-br-[100px] md:rounded-tl-[150px] md:rounded-br-[150px] overflow-hidden shadow-lg animate-float"
      />
    </div>
    
    <!-- Contenido -->
    <div class="space-y-5 text-center md:text-left animate-fade-in">
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold leading-tight">
        ONCLICK <span class="text-white">Social Media</span> <br class="hidden md:block" />
        <span class="text-yellow-400">Management</span>
      </h2>
      <p class="text-base sm:text-lg text-gray-200 max-w-xl mx-auto md:mx-0">
        Ayudamos a las marcas a crecer en redes sociales con contenido auténtico y estrategias personalizadas. Creamos videos, fotos y guiones que conectan con tu audiencia.
      </p>
      <a href="#paquetes" class="inline-block bg-yellow-400 text-[#0b1120] px-5 py-3 rounded-md font-semibold hover:bg-yellow-300 transition shadow-md">
        Conoce nuestros paquetes
      </a>
    </div>
  </div>
</section>


<!-- Estilos de animación -->
<style>
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-12px); }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  @keyframes fadeInUp {
    0% {
      opacity: 0;
      transform: translateY(24px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeInUp 0.8s ease-out forwards;
  }
</style>
